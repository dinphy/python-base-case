"""
    函数的嵌套
    变量的作用域
    闭包
    装饰器
        def wrapper(fn):
            def inner(*args, **kwargs);
                ret = fn(*args, **kwargs)
                return ret
            return inner

        @wrapper
        def func():
            pass
    迭代器
    生成器
        yield

        g = (x for x in xx)
    推导式
    匿名函数
    python内置函数_下 sorted, filter, map
"""
